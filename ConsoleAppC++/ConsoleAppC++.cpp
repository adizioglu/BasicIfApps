#include <iostream>

int main() {
    bool isComplete = true;

    if (isComplete) {
        std::cout << "The task is complete." << std::endl;
    }
    else {
        std::cout << "The task is not complete." << std::endl;
    }

    return 0;
}
